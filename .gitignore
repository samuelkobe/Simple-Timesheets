# Protect the real manifest.json — it contains your Google OAuth client ID.
# Commit manifest.example.json instead (it has the placeholder).
# When setting up locally: copy manifest.example.json → manifest.json and add your client ID.
manifest.json

# macOS
.DS_Store

# Editor
.vscode/
*.swp
